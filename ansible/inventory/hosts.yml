all:
  children:

    bastion_servers:
      hosts:
        bastion01:
          ansible_host: 13.201.7.13
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/cloud-devops-lab-2026

    app_servers:
      hosts:
        app01:
          ansible_host: 10.0.2.203
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/cloud-devops-recovery/bastion-recovery
          ansible_ssh_common_args: "-o ProxyJump=cloud-devops-bastion"
